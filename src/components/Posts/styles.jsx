import styled from "styled-components";

export const Posts = styled.div `
    display: flex;
    gap: 30px;
    justify-content: space-around;
    flex-wrap: wrap;
    margin: 50px 0;
    padding: 0 40px;
`